# Test App

## Setup

Add a `env.ts` file in the `example` folder with the following structure:
```properties
export const MOBILYFLOW_APP_ID = '';
export const MOBILYFLOW_API_KEY = '';
export const MOBILYFLOW_API_EXTRA_URLS: { label: string; value: string }[] = [];
```
